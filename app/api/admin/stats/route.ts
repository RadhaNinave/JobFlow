import {connectDB} from "@/lib/db";import User from "@/models/User";import Job from "@/models/Job";import Application from "@/models/Application";import {getSession} from "@/lib/auth";import {ok,fail} from "@/lib/response";export async function GET(){const s=await getSession();if(s?.role!=="admin")return fail("Admin access required.",403);await connectDB();const [jobs,users,applications]=await Promise.all([Job.countDocuments(),User.countDocuments({role:"user"}),Application.countDocuments()]);return ok({jobs,users,applications})}
